{
    "$id": "https://huwelijksplanner.nl/schemas/hp.price.schema.json",
    "$schema": "https://docs.commongateway.nl/schemas/Entity.schema.json",
    "version": "0.1",
    "type": "object",
    "title": "Price",
    "properties":{
        "id": {
            "type": "string",
            "format": "uuid"
        },
        "amount": {
            "type": "float",
            "nullable": true,
            "description":  "The price the message was billed with. It could have a null value if the message isn’t billed yet."
        },
        "currency": {
            "type": "string",
            "nullable": true,
            "description":  "The alphabetic code of the price currency, in ISO 4217 format. It could have null value if the message isn’t billed yet."
        }
    }
}
